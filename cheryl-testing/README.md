run main.py to get all current polymarket and kalshi markets (that expire on march 11th or later)
Runs fuzzy testing and embedding and saves the results to a json file
you can change the thresholds at the top